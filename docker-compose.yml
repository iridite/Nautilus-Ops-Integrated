version: '3.8'
services:
  sandbox:
    build: .
    container_name: nautilus_sandbox
    restart: unless-stopped
    volumes:
      - ./:/app:cached
    environment:
      - PYTHONUNBUFFERED=1
    command: uv run python -u sandbox/engine.py
    tty: true
