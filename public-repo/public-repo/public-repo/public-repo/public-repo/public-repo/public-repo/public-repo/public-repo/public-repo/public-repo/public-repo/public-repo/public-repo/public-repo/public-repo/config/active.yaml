# 当前活跃配置选择
# 这个文件控制当前使用的环境和策略

# 当前环境 (可选: dev, prod)
environment: "dev"

# 当前策略 (可选: oi_divergence, rs_squeeze, dual_thrust, kalman_pairs, keltner_rs_breakout)
strategy: "keltner_rs_breakout"

# 运行时覆盖配置 (可选)
# 这里的设置会覆盖环境和策略配置中的对应项
overrides:
  # 回测时间范围在 config/environments/{env}.yaml 中配置
  # backtest:
  #   start_date: "2022-01-01"
  #   end_date: "2026-01-01"

  # 可以覆盖交易设置
  trading:
    initial_balance: 100
    # oms_type: "HEDGING"  # 取消注释以覆盖持仓模式

  # 可以覆盖策略参数（仅用于调试）
  # strategy:
  #   max_positions: 3
  #   qty_percent: 0.05
